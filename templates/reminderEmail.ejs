<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      table {
        border-collapse: collapse;
      }
      table,
      td,
      th {
        border: 1px solid black;
      }
      th {
        background-color: lightgrey;
      }
      td,
      th {
        padding: 10px;
      }
    </style>
  </head>
  <body>
    <p>Dear <%=name%>,</p>
    <p>You have <%=tasks.length%> due today.</p>
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <%for (let i = 0; i < tasks.length; i++) {%> <% const task = tasks[i] %>
        <tr>
          <td><%=task.title%></td>
          <td><%=task.description%></td>
        </tr>
        <%}%>
      </tbody>
    </table>
  </body>
</html>
